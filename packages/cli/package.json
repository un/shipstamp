{
  "name": "@shipstamp/cli",
  "version": "0.0.0",
  "private": true,
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "files": [
    "dist"
  ],
  "dependencies": {
    "@shipstamp/core": "workspace:*"
  },
  "scripts": {
    "build": "tsup src/index.ts --format cjs --dts --sourcemap --clean",
    "typecheck": "tsc -p tsconfig.json --noEmit"
  }
}
